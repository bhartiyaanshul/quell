name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Opt into Node 24 for JavaScript-based actions.  GitHub flagged the
# v4 / v5 majors as Node-20 and deprecated in Sept 2025; this variable
# lets the same action versions run under the Node 24 runtime without
# waiting for every maintainer to cut a new major.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('poetry.lock') }}
          restore-keys: |
            venv-${{ runner.os }}-${{ matrix.python-version }}-

      - name: Install dependencies
        run: poetry install --no-interaction

      - name: Lint (ruff)
        run: poetry run ruff check quell/ tests/

      - name: Format check (ruff)
        run: poetry run ruff format quell/ tests/ --check

      - name: Type check (mypy)
        run: poetry run mypy quell/

      - name: Tests (pytest)
        run: poetry run pytest tests/ --tb=short -q
