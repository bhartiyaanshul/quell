"""Agents-graph tools — finish signals + inter-agent coordination.

Tools are registered via :func:`quell.tools.builtins.register_builtin_tools`.
Phase 13 adds ``create_agent`` / ``send_message`` / ``wait_for_message`` /
``view_graph`` to the same family.
"""
